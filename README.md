# AWS Repository

## Symbolic Links
- KeyFile 
`ln -s ./fchaconn.pem fchaconkey`

- Files path in instance 
`ln -s ../files/ modules`

- Module path in instance
`ln -s ../modules modules`

